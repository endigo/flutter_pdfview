# Workspace-root analyzer settings. There is no Dart source at the root, so
# this only pins the formatter page width for tooling invoked from here; each
# package under packages/ carries its own analysis_options.yaml with the full
# flutter_lints rule set.
formatter:
  page_width: 100
